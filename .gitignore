# Secrets
.env
.env.local
*.key

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Node / SvelteKit
node_modules/
.svelte-kit/
.vercel/
build/
dist/

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp

# Runtime artifacts (keep published data/*.json committed for the web app;
# cache + audit are local-only)
data/cache/
data/audit/
data/.last_scan_state.json
# Belt-and-braces: these files hold sensitive rolling state and should only
# ever live under data/cache/ (not data/). Explicitly block both locations.
data/alerts_log.jsonl
data/weekly_events.json
